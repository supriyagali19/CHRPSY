import os

huggingface_api_key = os.getenv("HUGGINGFACE_API_KEY")

if not huggingface_api_key:
    raise ValueError("Hugging Face API key is missing! Check GitHub Secrets.")

print("API Key Loaded Successfully!")  # Test if it's retrieved
